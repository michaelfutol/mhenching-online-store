# Server-only gate for the unauthenticated Quick Add design preview.
# Keep false in production until real Supabase auth/roles are implemented.
ADMIN_PREVIEW_ENABLED=false

# Future dedicated Mhenching Online Supabase values belong here after project creation.
# Never reuse the physical POS project's service-role key.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=
